---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<span
  class:list={[
    'inline-flex items-center rounded-button border border-border bg-surface-sunken px-3 py-1 font-mono text-xs font-medium text-secondary',
    className,
  ]}
>
  <slot />
</span>
