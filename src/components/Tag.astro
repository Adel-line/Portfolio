---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<span
  class:list={[
    'inline-flex items-center rounded-button bg-moss/15 px-3 py-1 text-xs font-medium text-moss-deep',
    className,
  ]}
>
  <slot />
</span>
